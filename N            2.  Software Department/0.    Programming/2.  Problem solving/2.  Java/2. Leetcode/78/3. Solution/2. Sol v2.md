```java title:"Leetcode - 78: "
	public List<List<Integer>> subsets(int[] nums) {
	
	   List<Integer> path = new ArrayList<>();
		List<List<Integer>> res = new ArrayList<>();
		int indx = 0;
	
		return backtrack(nums, indx);
	}
	
	public  List<List<Integer>> backtrack(int[] nums, int indx){
	
		if(indx == nums.length){
			List<List<Integer>> base = new ArrayList<>();
			List<Integer> list = new ArrayList<>();

			base.add(list);

			return base;
		}
	
		var rec = backtrack(nums, indx + 1);
		List<List<Integer>> res = new ArrayList<>();
	
		for(var each : rec){
			res.add(new ArrayList<>(each));
		}
	
		for(var each : rec){
			res.add(new ArrayList<>(each));
			var temp = res.get(res.size() - 1);
			temp.add(nums[indx]);
		}
		
		return res;
	
	}
```

