

My idea was simple that i had already been solved a problem like this way . where no duplicate number presence in array.

```java title:"print the target sum"
	public static void TargetSum(int strIndx, int length, int[] arr, int targetSum, int curntSum, String path){  
	    if(strIndx == length){  
	        if( curntSum == targetSum)  
	            println(path+" ");  
	          
	        return;   
	    }   
	      
	    int curntVal = arr[strIndx];  
	    TargetSum(strIndx+1, length, arr, targetSum, curntSum+curntVal, path+" "+curntVal);  
	    TargetSum(strIndx+1, length, arr, targetSum, curntSum, path);  
	}
```

so designed the a similar solution for **Leetcode - 40 Combination Sum II**
```java title:"Leetcode 40 Combination Sum II Wrong"
public List<List<Integer>> combinationSum2(int[] candidates, int target) {  
    List<Integer> path = new ArrayList<>();  
    combinationSum(candidates, 0, target, 0, path);  
    Collections.sort(res);  
      
    return res;   
}  
  
public static List<List<Integer>> res = new ArrayList<>();  
public void combinationSum(int[] candidates, int indx, int targetSum, int curntSum, List<Integer> path){  
    if(curntSum == targetSum){  
        Collections.sort(path);  
        res.add( new ArrayList<>(path) );   
          
        return;   
    }   
      
    if(indx == candidates.length){  
        return;   
    }  
      
    path.add(candidates[indx]);  
    combinationSum(candidates, indx + 1, targetSum, curntSum+candidates[indx], path);  
    path.remove(path.size() - 1);  
    combinationSum(candidates, indx+1, targetSum, curntSum, path);   
}

```
