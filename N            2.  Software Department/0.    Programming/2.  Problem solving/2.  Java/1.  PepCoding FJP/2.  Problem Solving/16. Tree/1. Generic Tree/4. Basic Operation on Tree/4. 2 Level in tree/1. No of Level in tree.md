```java title:"No of Level in tree"
public int Level(TreeNode node){
	 int level = 0;
	 for(var eachChild : node.children){
		 levle = Math.Max(level,Level(eachChild));
	 }
	 
	 return (level+1)
}
```

